apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafanapvc
  namespace: loki-stack
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
